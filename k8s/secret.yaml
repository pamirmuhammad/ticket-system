apiVersion: v1
kind: Secret
metadata:
  name: ticket-system-secret
  namespace: ticket-system
type: Opaque
stringData:
  DB_USERNAME: "postgres"
  DB_PASSWORD: ""  # Must be set in production — use `kubectl create secret` or external secrets
  JWT_SECRET: ""  # Must be set in production (min 32 chars)
  SENTRY_DSN: ""
  MAIL_USERNAME: ""
  MAIL_PASSWORD: ""
